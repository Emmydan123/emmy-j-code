const fs=require('fs');const path=require('path');const mysql=require('mysql2/promise');require('dotenv').config();
(async()=>{const cfg={host:process.env.DB_HOST||'localhost',port:Number(process.env.DB_PORT||3306),user:process.env.DB_USER||'root',password:process.env.DB_PASSWORD||'',multipleStatements:true};let c;
try{c=await mysql.createConnection(cfg);console.log('Connected to MySQL.');const files=['schema.sql','assessment-migration.sql','teacher-admin-migration.sql','learning-experience-migration.sql','programming-payment-migration.sql'];for(const f of files){const sql=fs.readFileSync(path.join(__dirname,'..','sql',f),'utf8');console.log('Applying',f);await c.query(sql);}console.log('Database setup complete.');await c.end();}catch(e){console.error('\nDATABASE SETUP FAILED');console.error(e.message);console.error('\nCheck that MySQL is running and .env contains the correct DB_USER/DB_PASSWORD.');process.exit(1)}})();
