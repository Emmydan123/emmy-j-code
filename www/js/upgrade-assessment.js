const bank={intermediate:[
['Which shortcut copies selected text?','Ctrl+C'],['Which Word feature creates a numbered list?','Numbering'],['Which Excel function adds a range of numbers?','SUM'],['Which email field hides recipients from each other?','BCC'],['Which browser feature saves a page for quick return?','Bookmark'],['What should you do before removing a USB drive?','Safely eject it'],['What device commonly connects a local network to the internet?','Router'],['What is a useful first step when a printer will not print?','Check power, connection and print queue'],['What does MFA add to account security?','An additional verification factor'],['What does Ctrl+F normally do?','Find text']],
advanced:[
['What service resolves a domain name to an IP address?','DNS'],['What principle gives users only the access they need?','Least privilege'],['What should a recovery plan include?','Documented recovery steps and tested backups'],['What is a useful tool for checking reachability to a host?','Ping'],['Why monitor CPU, memory and disk usage?','To identify resource bottlenecks'],['What is a rollback plan?','A documented way to safely reverse a change'],['Why segment a network?','To limit traffic and reduce risk'],['What is MFA?','Multi-factor authentication'],['Why test restored backups?','To verify recovery actually works'],['What should be reviewed before a production configuration change?','Risk, dependencies, approval and rollback plan']]
};
const target=new URLSearchParams(location.search).get('target')||'intermediate'; const qs=bank[target]||bank.intermediate;
function render(){document.querySelector('#questions').innerHTML=qs.map((q,i)=>`<fieldset class="question"><legend><b>${i+1}. ${q[0]}</b></legend><label class="option"><input type="radio" name="q${i}" value="1"> ${q[1]}</label><label class="option"><input type="radio" name="q${i}" value="0"> I need to review this skill</label></fieldset>`).join('')}
async function submit(){let c=0,a={};qs.forEach((q,i)=>{const v=document.querySelector(`input[name=q${i}]:checked`)?.value||'';a[i]=v;if(v==='1')c++});const score=Math.round(c/qs.length*100);const out=document.querySelector('#result');try{const r=await EmmyAuth.request('/api/level-upgrade',{method:'POST',body:JSON.stringify({targetLevel:target,score,answers:a})});out.innerHTML=r.passed?`<div class="notice success"><b>Level upgrade passed — ${score}% 🎉</b><br>You are now in the ${target} curriculum.</div><a class="btn btn-primary" href="dashboard.html">Continue →</a>`:`<div class="notice error"><b>${score}% — not passed.</b><br>You need 80%. Review your current level and try again.</div><button class="btn btn-secondary" onclick="location.reload()">Try again</button>`}catch(e){out.innerHTML=`<div class="notice error">${e.message}</div>`}}
document.addEventListener('DOMContentLoaded',()=>{document.querySelector('#target').textContent=target[0].toUpperCase()+target.slice(1);render();document.querySelector('#submit').onclick=submit});
