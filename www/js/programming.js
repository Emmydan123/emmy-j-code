async function api(url,opt={}){const r=await fetch((window.EMMY_API_URL || ((location.hostname==='localhost'||location.hostname==='127.0.0.1')&&location.port!=='3000'?'http://localhost:3000':''))+url,{credentials:'include',headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d}
async function load(){const a=document.getElementById('access'),p=document.getElementById('payment'),m=document.getElementById('modules');try{const d=await api('/api/programming/access'); if(!d.eligible){a.innerHTML='<strong>🔒 Programming locked</strong><br>'+d.reason; p.innerHTML=d.paymentInstructions||''; if(d.canSubmit){p.innerHTML += '<form id="payForm" class="card" style="margin-top:12px"><h3>Submit payment for verification</h3><label>Payment method<input name="method" required placeholder="e.g. Bank transfer"></label><label>Payment reference<input name="reference" required placeholder="Enter your transaction/reference number"></label><label>Amount<input name="amount" type="number" min="0" step="0.01" placeholder="Amount paid"></label><label>Note<textarea name="note" placeholder="Optional note"></textarea></label><button class="btn btn-primary" type="submit">Submit for verification</button><p id="payMsg" class="muted"></p></form>';document.getElementById('payForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await api('/api/programming/payment',{method:'POST',body:JSON.stringify({method:f.get('method'),reference:f.get('reference'),amount:f.get('amount'),note:f.get('note')})});document.getElementById('payMsg').textContent='Payment submitted. An administrator must verify it before access is granted.';e.target.reset()}catch(x){document.getElementById('payMsg').textContent=x.message}}}return}a.innerHTML='<strong>🔓 Programming unlocked</strong><br>You have access to the Programming Academy.';const c=await fetch('../data/programming-curriculum.json').then(r=>r.json());m.innerHTML=c.modules.map((x,i)=>`<section class="card" style="margin-top:14px"><span class="badge">Module ${i+1}</span><h2>${x.title}</h2><p class="muted">${x.lessons.length} practical lessons</p><a class="btn btn-primary" href="lesson.html?module=${encodeURIComponent(x.id)}">Start module</a></section>`).join('')}catch(e){a.textContent=e.message}}
load();
